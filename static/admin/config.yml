backend:
  name: git-gateway
  branch: main
publish_mode: editorial_workflow
media_folder: assets/img/uploads

collections:
  - name: docs_chaster
    label: Docs - Chaster
    folder: docs
    preview_path: '{{slug}}'
    create: true
    extension: mdx
    format: frontmatter
    fields:
      - { name: title, label: Title, widget: string }
      - { name: sidebar_label, label: Sidebar label, widget: string }
      - { name: body, label: Body, widget: markdown }
  - name: docs_locks
    label: Docs - Locks
    folder: docs/locks
    preview_path: 'locks/{{slug}}'
    create: true
    extension: mdx
    format: frontmatter
    fields:
      - { name: title, label: Title, widget: string }
      - { name: sidebar_label, label: Sidebar label, widget: string }
      - { name: body, label: Body, widget: markdown }
  - name: docs_features
    label: Docs - Features
    folder: docs/features
    preview_path: 'features/{{slug}}'
    create: true
    extension: mdx
    format: frontmatter
    fields:
      - { name: title, label: Title, widget: string }
      - { name: sidebar_label, label: Sidebar label, widget: string }
      - { name: body, label: Body, widget: markdown }
  - name: docs_activity
    label: Docs - Activity
    folder: docs/activity
    preview_path: 'activity/{{slug}}'
    create: true
    extension: mdx
    format: frontmatter
    fields:
      - { name: title, label: Title, widget: string }
      - { name: sidebar_label, label: Sidebar label, widget: string }
      - { name: body, label: Body, widget: markdown }
  - name: docs_extensions
    label: Docs - Extensions
    folder: docs/extensions
    preview_path: 'extensions/{{slug}}'
    create: true
    extension: mdx
    format: frontmatter
    fields:
      - { name: title, label: Title, widget: string }
      - { name: sidebar_label, label: Sidebar label, widget: string }
      - { name: body, label: Body, widget: markdown }
